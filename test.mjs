import { PGlite } from '@electric-sql/pglite'; const db = new PGlite('./local.db'); try { await db.query('select "id", "clerk_user_id", "name", "user_type", "role", "role_name", "title", "email", "workspace_id", "phone", "avatar_url", "department_id", "status", "employee_code", "staff_type", "joined_at", "manager_id", "teaching_level", "subject", "homeroom_class_id", "email_verified_at", "password_changed_at", "must_change_password", "internal_note", "data_scope", "two_factor_enabled", "last_login_at", "deleted_at", "created_by", "updated_by", "department_scope", "payload", "created_at", "updated_at" from "users" where "users"."id" = $1 limit $2', ['user_chutich', 1]); console.log('Success'); } catch (e) { console.error('ERROR:', e.message); } process.exit(0);
